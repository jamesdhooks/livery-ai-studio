"""
server/
-------
Backend package for Livery AI Studio.

Modules:
    config   — app paths, config load/save, defaults
    cars     — car library scanning, caching, lookup
    generate — Gemini AI livery generation
    deploy   — iRacing paint folder deployment
    extract  — PSD/zip template extraction
    upscale  — Real-ESRGAN GPU upscaling
    routes/  — Flask blueprint modules
"""
