"""
server/routes/
--------------
Flask Blueprint modules, one per functional area.

Blueprints:
    api_config   — /api/config, /api/session, /api/version, /api/log
    api_cars     — /api/cars, /api/library
    api_generate — /api/generate, /api/sponsors, /api/enhance-prompt
    api_history  — /api/history, /api/deploy, /api/upscale, /api/preview
    api_files    — /api/upload-file, /api/uploads, /api/browse-uploads, /api/pick-*
    api_window   — /api/window/*
"""
